using UnityEngine;

public struct MetaBallStruct
{
    public Vector2 Position;
    public float Radius;

    public MetaBallStruct(Vector2 position, float radius)
    {
        Position = position;
        Radius = radius;
    }
}
